- input: SEL|
  want:
    candidates:
      tokens: [SELECT]
      slots: []

- input: SELECT u.| FROM users AS u
  want:
    candidates:
      tokens: ['*']
      slots: [Column, Function]
    qualifier: [u]
    scope:
      local: [users AS u]

- input: SELECT |
  want:
    candidates:
      tokens: [(, '*', +, '-', ALL, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, DISTINCT, EXCEPT, EXISTS, EXTRACT, 'FALSE', FETCH, FOR, FROM, GREATEST, GROUP, GROUPING, HAVING, INTERSECT, INTO, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LIMIT, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OFFSET, OPERATOR, ORDER, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', UNION, USER, WHERE, WINDOW, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM |
  want:
    candidates:
      tokens: [(, GRAPH_TABLE, JSON_TABLE, LATERAL, ONLY, ROWS, XMLTABLE]
      slots: [Function, Relation]

- input: SELECT * FROM public.orders o|
  want:
    candidates:
      tokens: []
      slots: [Alias]
    scope:
      local: [public.orders AS o]

- input: SELECT * FROM public.orders AS o|
  want:
    candidates:
      tokens: []
      slots: [Alias]
    scope:
      local: [public.orders AS o]

- input: SELECT * FROM public.orders AS o j|
  want:
    candidates:
      tokens: [JOIN]
      slots: []
    scope:
      local: [public.orders AS o]

- input: SELECT * FROM (|
  want:
    candidates:
      tokens: [(, ), GRAPH_TABLE, JSON_TABLE, LATERAL, ONLY, ROWS, SELECT, TABLE, VALUES, WITH, XMLTABLE]
      slots: [Function, Relation]

- input: SELECT * FROM (SELECT |
  want:
    candidates:
      tokens: [(, ), '*', +, '-', ALL, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, DISTINCT, EXCEPT, EXISTS, EXTRACT, 'FALSE', FETCH, FOR, FROM, GREATEST, GROUP, GROUPING, HAVING, INTERSECT, INTO, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LIMIT, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OFFSET, OPERATOR, ORDER, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', UNION, USER, WHERE, WINDOW, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM users JOIN |
  want:
    candidates:
      tokens: [(, GRAPH_TABLE, JSON_TABLE, LATERAL, ONLY, ROWS, XMLTABLE]
      slots: [Function, Relation]

- input: WITH |
  want:
    candidates:
      tokens: [RECURSIVE]
      slots: [AnyName]

- input: WITH x AS (|
  want:
    candidates:
      tokens: [(, ), DELETE, INSERT, MERGE, SELECT, TABLE, UPDATE, VALUES, WITH]
      slots: []

- input: WITH x AS (SELECT |
  want:
    candidates:
      tokens: [(, ), '*', +, '-', ALL, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, DISTINCT, EXCEPT, EXISTS, EXTRACT, 'FALSE', FETCH, FOR, FROM, GREATEST, GROUP, GROUPING, HAVING, INTERSECT, INTO, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LIMIT, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OFFSET, OPERATOR, ORDER, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', UNION, USER, WHERE, WINDOW, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: (|
  want:
    candidates:
      tokens: [(, SELECT, TABLE, VALUES, WITH]
      slots: []

- input: VALUES |
  want:
    candidates:
      tokens: [(]
      slots: []

- input: TABLE |
  want:
    candidates:
      tokens: [ONLY]
      slots: [Relation]

- input: SELECT * FROM t WHERE |
  want:
    candidates:
      tokens: [(, +, '-', ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM t WHERE EXISTS (SELECT |
  want:
    candidates:
      tokens: [(, '*', +, '-', ALL, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, DISTINCT, EXCEPT, EXISTS, EXTRACT, 'FALSE', FETCH, FOR, FROM, GREATEST, GROUP, GROUPING, HAVING, INTERSECT, INTO, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LIMIT, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OFFSET, OPERATOR, ORDER, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', UNION, USER, WHERE, WINDOW, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM t JOIN u ON |
  want:
    candidates:
      tokens: [(, +, '-', ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT 1 UNION |
  want:
    candidates:
      tokens: [(, ALL, DISTINCT, SELECT, TABLE, VALUES]
      slots: []

- input: SELECT "Mi| FROM t
  want:
    candidates:
      tokens: []
      slots: [Column, Function]

- input: SELECT 1; SEL|
  want:
    candidates:
      tokens: [SELECT]
      slots: []

- input: SELECT 1::|
  want:
    candidates:
      tokens: []
      slots: [Type]

- input: SELECT 1 COLLATE |
  want:
    candidates:
      tokens: []
      slots: [Collation]

- input: SELECT * FROM JSON_TABLE(|
  want:
    candidates:
      tokens: [(, +, '-', ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM XMLTABLE(|
  want:
    candidates:
      tokens: [(, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, 'NULL', NULLIF, OVERLAY, POSITION, ROW, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLNAMESPACES, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE]
      slots: [Column, Function]

- input: SELECT * FROM ROWS FROM (lower(|
  want:
    candidates:
      tokens: [(, ), '*', +, '-', ALL, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, DISTINCT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, VARIADIC, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM generate_series(|
  want:
    candidates:
      tokens: [(, ), '*', +, '-', ALL, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, DISTINCT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, VARIADIC, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM XMLTABLE('/x' PASSING doc COLUMNS c text DEFAULT |
  want:
    candidates:
      tokens: [(, +, '-', ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM JSON_TABLE(doc, '$' COLUMNS (c int DEFAULT |
  want:
    candidates:
      tokens: [(, +, '-', ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM t OFFSET lower(|
  want:
    candidates:
      tokens: [(, ), '*', +, '-', ALL, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, DISTINCT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, VARIADIC, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM t FETCH FIRST lower(|
  want:
    candidates:
      tokens: [(, ), '*', +, '-', ALL, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, DISTINCT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, VARIADIC, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT JSON_ARRAY(SELECT 1 FORMAT |
  want:
    candidates:
      tokens: [JSON]
      slots: []

- input: SELECT JSON_ARRAY(SELECT 1 RETURNING |
  want:
    candidates:
      tokens: []
      slots: [Type]

- input: SELECT sum(x) OVER (|
  want:
    candidates:
      tokens: [), GROUPS, ORDER, PARTITION, RANGE, ROWS]
      phrases: [ORDER BY, PARTITION BY]
      slots: [AnyName]

- input: SELECT sum(x) OVER (PARTITION BY |
  want:
    candidates:
      tokens: [(, +, '-', ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT 1 OPERATOR(|
  want:
    candidates:
      tokens: [)]
      slots: [Operator]

- input: SELECT CAST(x AS |)
  want:
    candidates:
      tokens: []
      slots: [Type]

- input: SELECT 1|
  want:
    candidates:
      tokens: ['%', '*', +, ',', '-', /, <, =, '>', AND, AS, AT, BETWEEN, COLLATE, EXCEPT, FETCH, FOR, FROM, GROUP, GreaterEquals, HAVING, ILIKE, IN, INTERSECT, INTO, IS, ISNULL, LIKE, LIMIT, LessEquals, NOT, NOTNULL, NotEquals, OFFSET, OPERATOR, OR, ORDER, Op, RightArrow, SIMILAR, TypeCast, UNION, WHERE, WINDOW, ^, '|']
      phrases: [GROUP BY, ORDER BY]
      slots: [Alias]

- input: SELECT * FROM t WHERE 1|
  want:
    candidates:
      tokens: ['%', '*', +, '-', /, <, =, '>', AND, AT, BETWEEN, COLLATE, EXCEPT, FETCH, FOR, GROUP, GreaterEquals, HAVING, ILIKE, IN, INTERSECT, IS, ISNULL, LIKE, LIMIT, LessEquals, NOT, NOTNULL, NotEquals, OFFSET, OPERATOR, OR, ORDER, Op, RightArrow, SIMILAR, TypeCast, UNION, WINDOW, ^, '|']
      phrases: [GROUP BY, ORDER BY]
      slots: []

- input: SELECT * FROM left_table JOIN right_table USING (|)
  want:
    candidates:
      tokens: []
      slots: [Column]

- input: SELECT * FROM sampled TABLESAMPLE |
  want:
    candidates:
      tokens: []
      slots: [Function]

- input: SELECT * FROM GRAPH_TABLE(| MATCH (v) COLUMNS (v.id))
  want:
    candidates:
      tokens: []
      slots: [PropertyGraph]

- input: SELECT * FROM (left_table AS l JOIN |)
  want:
    candidates:
      tokens: [(, ), GRAPH_TABLE, JSON_TABLE, LATERAL, ONLY, ROWS, XMLTABLE]
      slots: [Function, Relation]

- input: SELECT JSON_ARRAYAGG(value ORDER BY |) FROM source_table
  want:
    candidates:
      tokens: [(, +, '-', ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, EXISTS, EXTRACT, 'FALSE', GREATEST, GROUPING, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OPERATOR, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', USER, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]

- input: SELECT * FROM accounts a WHERE EXISTS (SELECT 1 FROM orders o WHERE o.id = a.|)
  want:
    candidates:
      tokens: ['*']
      slots: [Column, Function]
    qualifier: [a]
    scope:
      local: [orders AS o]
      outer: [[accounts AS a]]

- input: SELECT * FROM accounts a, LATERAL (SELECT a.|) s
  want:
    candidates:
      tokens: [), '*']
      slots: [Column, Function]
    qualifier: [a]
    scope:
      outer: [[accounts AS a]]

- input: WITH x(a) AS (SELECT 1) SELECT | FROM x
  want:
    candidates:
      tokens: [(, '*', +, '-', ALL, ARRAY, CASE, CAST, COALESCE, COLLATION, CURRENT_CATALOG, CURRENT_DATE, CURRENT_ROLE, CURRENT_SCHEMA, CURRENT_TIME, CURRENT_TIMESTAMP, CURRENT_USER, DEFAULT, DISTINCT, EXCEPT, EXISTS, EXTRACT, 'FALSE', FETCH, FOR, FROM, GREATEST, GROUP, GROUPING, HAVING, INTERSECT, INTO, JSON, JSON_ARRAY, JSON_ARRAYAGG, JSON_EXISTS, JSON_OBJECT, JSON_OBJECTAGG, JSON_QUERY, JSON_SCALAR, JSON_SERIALIZE, JSON_VALUE, LEAST, LIMIT, LOCALTIME, LOCALTIMESTAMP, MERGE_ACTION, NORMALIZE, NOT, 'NULL', NULLIF, OFFSET, OPERATOR, ORDER, OVERLAY, Op, POSITION, ROW, RightArrow, SESSION_USER, SUBSTRING, SYSTEM_USER, TREAT, TRIM, 'TRUE', UNION, USER, WHERE, WINDOW, XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE, '|']
      slots: [Column, Function]
    scope:
      local: [cte x(a)]
      ctes: [x(a)]

- input: SELECT * FROM (SELECT 1) AS s, generate_series(x|, 10)
  want:
    candidates:
      tokens: [XMLCONCAT, XMLELEMENT, XMLEXISTS, XMLFOREST, XMLPARSE, XMLPI, XMLROOT, XMLSERIALIZE]
      slots: [Column, Function]
    scope:
      local: [subquery AS s]

- input: SELECT * FROM accounts FOR UPDATE OF |
  want:
    candidates:
      tokens: []
      slots: [Relation]
    scope:
      local: [accounts]

- input: SELECT * FROM users AS u WHERE lower(u.na|)
  want:
    candidates:
      tokens: []
      slots: [Column, Function]
    qualifier: [u]
    scope:
      local: [users AS u]
